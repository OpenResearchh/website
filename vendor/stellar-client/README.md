# @openresearch/stellar-client (vendored)

Vendored build of the OpenResearch Soroban TypeScript client (`packages/client`
from the smart-contracts repo). Consumed via a `file:` dependency.

To update: rebuild the client (`npm run build`) in smart-contracts and copy its
`dist/` and `package.json` here.
